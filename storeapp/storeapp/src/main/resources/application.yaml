spring:
  datasource:
    password: ROOT
    url: jdbc:mysql://localhost:3306/storeapp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    show-sql: true